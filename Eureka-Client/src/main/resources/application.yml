server:
  port: 8762

spring:
  application:
    name: eureka-client

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    status-page-url: http://localhost:8762/swagger-ui.html

# ${server.port}为该服务的端口号
#eureka.instance.status-page-url=http://localhost:${server.port}/swagger-ui.html